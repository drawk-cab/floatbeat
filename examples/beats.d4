( beats ) ::beat;

1 clip
4 96bpm 2* beat

from
kick,
tick,
dup kick hat,
tick
choose

(instruments: age -- SOUND)

:kick
    dup 0.7< if
        dup 0.7~ 0.7/
        swap 2*
        70 Hz
        swap dup* 1~ 4* +
        T sin 1.2** .
    else
        drop
    then
;


:hat
    dup 0.5< if
        0.43~
        noise* 5/.
    else
        drop
    then
;

:tick
    dup 0.1< if
        0.1~
        noise* 3/.
    else
        drop
    then
;
