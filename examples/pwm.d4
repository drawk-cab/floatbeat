( Lead synth with pulse width modulation )

:: scale ticks env;

8 280bpm ticks swap
from G low, B', G, D, D', C, E, G choose
pwm.

(instruments)

:pwm  (age freq -- signal)
  over            (-- age freq age)
  (age) 5Hz sin   (-- age freq lfo)
  0.2* 0.25+      (-- age freq pulse-width)
  pulse
;
